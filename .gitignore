# secrets / local config — NEVER commit
.env
.env.local
.dev.vars

# deps / build
node_modules/
.wrangler/
dist/
.next/

# os / editor
.DS_Store
*.log
.gstack/

# bun (we standardize on npm; gstack's own CLI uses bun separately)
bun.lock
bun.lockb

# installed agent skills (reproducible via: npx skills add ...)
.agents/
skills-lock.json
