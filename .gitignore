# secrets / local config — NEVER commit
.env
.env.local
.dev.vars

# deps / build
node_modules/
.wrangler/
dist/
.next/

# os / editor
.DS_Store
*.log
.gstack/

# bun lockfiles (CI uses npm)
bun.lock
bun.lockb
