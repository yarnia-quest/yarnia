{
  "name": "yarnia-marketing",
  "private": true,
  "type": "module",
  "description": "Yarnia marketing: static landing page (public/) served by a Cloudflare Worker with Static Assets. No secrets; the signup is written client-side via @instantdb/core.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
