{
  "name": "yarnia-instant",
  "private": true,
  "type": "module",
  "description": "InstantDB schema + permissions as code (applied by .github/workflows/push-schema.yml)",
  "dependencies": {
    "@instantdb/core": "latest"
  }
}
